{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "launch and serve",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceRoot}",
      "server": {
        "command": "npm start"
      }
    },
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceRoot}",
    }
  ]
}
