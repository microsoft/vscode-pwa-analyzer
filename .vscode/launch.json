{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "webpack-dev-server",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceRoot}",
      "preLaunchTask": "npm: start"
    }
  ]
}
